---
export interface Props {
    text: string;
    iconClass: string;
}
const { text, iconClass } = Astro.props;
---

<h1
    class="text-2xl lg:text-3xl md:text-md mb-5 mt-10 tracking-wide"
    style="font-family: primous"
>
    <i class:list={["nf", iconClass]}>{text}</i>
</h1>
